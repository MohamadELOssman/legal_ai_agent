# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Virtual environments ──
venv/
.venv/
env/

# ── Secrets / config ──
.env
*.env
!.env.example

# ── OS / editor ──
.DS_Store
*.swp
.idea/
.vscode/

# ── Logs ──
logs/
*.log

# ── Notebooks ──
.ipynb_checkpoints/

# ── Evaluation / experiment outputs ──
eval_*.json
comparison_*.json
benchmark_*.json
retrieval_eval_*.json
embedding_study_*.json
chunking_study_*.json
study_*.json
_smoke*.json

# ── Derived artifacts (reproducible via scripts/build_index.py) ──
# The Chroma vector store is rebuilt from the processed JSON sources; don't track
# its large, churning binaries.
code/data_processed/vectorstore/
