# Runtime dependencies for the PUBLIC Streamlit app.
# Streamlit Community Cloud reads THIS repo-root file. It is intentionally minimal:
# only what public_app.py needs at runtime, with flexible bounds so wheels resolve on
# Python 3.13. It deliberately EXCLUDES build-only / unused packages that broke the
# earlier build — pymupdf & pypdf (PDF extraction; needs source compile) and the
# Google/OpenAI/langgraph providers (the public chat uses Anthropic only; those imports
# are now lazy). Set the app's Python version to 3.13 in Streamlit Cloud settings.
streamlit>=1.40,<2
anthropic>=0.39
langchain>=0.3.7,<0.4
langchain-anthropic>=0.2.4
langchain-community>=0.3.7,<0.4
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.2
chromadb>=0.5.3
sentence-transformers>=2.6,<4
rank-bm25>=0.2.2
pandas>=2.0
openpyxl>=3.1
markdown>=3.5
huggingface_hub>=0.24
pydantic>=2.7.4
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
loguru>=0.7
