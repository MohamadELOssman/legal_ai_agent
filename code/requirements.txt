# Runtime dependencies for the PUBLIC Streamlit app (public_app.py).
# Streamlit Community Cloud reads THIS file (it sits next to the entrypoint).
# Minimal + flexible so wheels resolve cleanly on Python 3.12/3.13.
#
# The full pinned developer list (PDF/OCR/spaCy/exact pins for local build_index,
# preprocessing, benchmarks, etc.) lives in requirements-dev.txt. Runtime-only here:
# no pymupdf/pypdf/pdfplumber/spacy/torch-pin, and Google/OpenAI/langgraph are omitted
# because those provider imports are lazy and the public chat uses Anthropic only.
streamlit>=1.40,<2
anthropic>=0.39
langchain>=0.3.7,<0.4
langchain-anthropic>=0.2.4
langchain-community>=0.3.7,<0.4
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.2
chromadb>=0.5.3
sentence-transformers>=2.6,<4
rank-bm25>=0.2.2
pandas>=2.0
openpyxl>=3.1
markdown>=3.5
huggingface_hub>=0.24
pydantic>=2.7.4
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
loguru>=0.7
